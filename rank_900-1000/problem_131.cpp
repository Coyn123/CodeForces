#include <iostream>
#include <string>
#include <vector>

class Solution {

    public:
    void solve() {
      std::string input;
      std::cin << input;
      // Check if first is lower AND rest is upper
      // If not, is it all upper?
      // if not -> return the same string? (nothing specified otherwise)
      // if is -> fix and return string (Xxx) not->(xXX) or (XXX)

    };
};

int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);

    Solution obj;
    obj.solve();
}
